{
  "name": "a3-foam-tree",
  "private": true,
  "version": "0.1.0",
  "description": "Modern Canvas/D3-Delaunay FoamTree-style hierarchical cluster browser.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest",
    "test:run": "vitest run",
    "check": "npm run test:run && npm run build"
  },
  "dependencies": {
    "d3-delaunay": "^6.0.4"
  },
  "devDependencies": {
    "@types/d3-delaunay": "^6.0.4",
    "playwright": "^1.61.1",
    "typescript": "^5.8.3",
    "vite": "^7.0.1",
    "vitest": "^4.1.9"
  }
}
